# ============================================================
# Academic-Literature Agentic RAG — Python dependencies
# Phase 1 baseline. Pinned to compatible-release versions.
# ============================================================

# --- Core orchestration ---
# Note: langchain-core 1.x is required by langgraph >= 0.6; pin langchain-* to 0.3.78+
# compatible releases. Install with `pip install -r requirements.txt` to resolve.
langgraph>=0.6.0
langchain>=0.3.30
langchain-core>=0.3.78
langchain-community>=0.3.30
langchain-chroma>=0.2.0
langchain-openai>=0.3.0       # Default LLM integration (OpenAI-compatible base_url)

# --- Vector store ---
chromadb>=0.5.5

# --- Embeddings & reranking (local sentence-transformer models) ---
sentence-transformers>=3.0.0
transformers>=4.44.0
torch>=2.2.0
accelerate>=0.34.0

# --- Sparse retrieval (BM25) ---
rank-bm25>=0.2.2

# --- PDF parsing ---
pypdf>=4.0.0

# --- Token counting & numerics ---
tiktoken>=0.7.0
numpy>=1.26.0
scikit-learn>=1.4.0

# --- Data schemas & config ---
pydantic>=2.7.0
PyYAML>=6.0.1
python-dotenv>=1.0.1

# --- Evaluation ---
ragas>=0.1.16

# --- Logging / console ---
rich>=13.7.0

# --- Frontend (Chainlit chat UI) ---
chainlit>=2.0.0

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
