# ===== OpenAI 接入配置 =====
# 用法:将下面 OPENAI_API_KEY= 右侧填入你的真实 key(等号后紧跟 key,不要留空格),
#       保存文件即生效 —— 系统会自动从离线切换为在线,无需改任何代码。
# 获取地址: https://platform.openai.com/api-keys
#
# 示例(替换 xxx):
#   OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
#
# 当前为空值 → 系统运行在离线模式;填入真实 key 后 → 自动在线。
OPENAI_API_KEY=your real key

# ----- 可选:切换到 OpenAI 兼容服务 -----
# 取消注释并填入对应值。改完后请同步修改 config/config.yaml 的 agent.llm.model 为目标模型名。
#
# DeepSeek:
#   OPENAI_API_KEY=你的deepseek-key
#   OPENAI_BASE_URL=https://api.deepseek.com/v1
#   (config: model → deepseek-chat)
#
# 本地 Ollama(无需真实 key):
#   OPENAI_API_KEY=ollama
#   OPENAI_BASE_URL=http://localhost:11434/v1
#   (config: model → 你的本地模型名,如 llama3)
#
# 本地 vLLM:
#   OPENAI_BASE_URL=http://localhost:8000/v1
#
OPENAI_BASE_URL=https://api.siliconflow.cn/v1
